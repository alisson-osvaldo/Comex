package br.com.comex.csv;

import java.io.FileReader;

import org.json.simple.JSONObject;
import org.json.simple.parser.JSONParser;


public class TesteJson {
	public static void main(String[] args) {
		
		/*  Obs: Trabalhando no Código ainda não está funcional!!!  */
/*
		JSONObject jsonObject;
		//Cria o parse de tratamento
		JSONParser parser = new JSONParser();
		
		//Variaveis que irao armazenar os dados do arquivo JSON
		String categoria;
	    String produto;
	    String cliente;
	    String preco;
	    String quantidade;
	    String data;
	    
	    try {
			//Salva no objeto JSONObject o que o parse tratou do arquivo
			jsonObject = (JSONObject) parser.parse(new FileReader("pedidos.json"));
			
			categoria = (String) jsonObject.get("categoria");
			produto = (String) jsonObject.get("produto");
			cliente = (String) jsonObject.get("cliente");
			preco = (String) jsonObject.get("preco");
			quantidade = (String) jsonObject.get("quantidade");
			data = (String) jsonObject.get("data");
			
			System.out.printf(
					"Categoria: %s\nproduto: %s\ncliente: %s\npreco: %s\nquantidade: %s\ndata: ",
					categoria, produto, cliente, preco, quantidade, data);
			
	    }catch (Exception e) {
			e.printStackTrace();
		} 
*/
	}
	
}










